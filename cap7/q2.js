// Crie um objeto carro com as propriedades marca, modelo e ano. Em
// seguida, imprima a marca do carro.
function run() {
  const carro = {
    marca: "Fiat",
    modelo: "Uno",
    ano: 2010,
  };

  console.log(carro.marca);
}

module.exports = { run };
